stages:
  - test

variables:
  SOLUTION_PATH: backend

before_script:
  - 'dotnet restore $SOLUTION_PATH/Solution.sln'

test:
  stage: test
  only:
    - main
  script:
    - 'dotnet test $SOLUTION_PATH/Solution.sln'
